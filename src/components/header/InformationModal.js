import React from "react";
import styled from "styled-components";

const ModalTemplate = styled.div`
`;

const InformationModal = () => {
    return (
        // 유저 정보를 보여주는 modal창부터
        <ModalTemplate>
        </ModalTemplate>
    );
}

export default InformationModal;